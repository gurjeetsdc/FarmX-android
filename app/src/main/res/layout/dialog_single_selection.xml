<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.sdei.farmx.R"/>
        <import type="android.view.View" />

        <variable
            name="itemClickListener"
            type="com.sdei.farmx.callback.RecyclerCallback" />

        <variable
            name="item"
            type="com.sdei.farmx.dataobject.RecyclerBindingList" />

        <variable
            name="header"
            type="String" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:gravity="center"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical">

            <LinearLayout
                android:id="@+id/header_ll"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="@{(header != null) ? View.VISIBLE : View.GONE}">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_margin="@dimen/dimen_20"
                    android:padding="@dimen/dimen_8"
                    android:text="@{header}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/dimen_36" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="@color/black" />

            </LinearLayout>

            <android.support.v7.widget.RecyclerView
                android:id="@+id/recycler_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:onItemClickListener="@{itemClickListener}"
                app:recyclerLinearAdapter="@{item}"
                app:layout="@{R.layout.row_single_selection}"/>

        </LinearLayout>

    </LinearLayout>

</layout>
